{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceRoot}/demo"
            },
            "args": [
                ""
            ],
            "problemMatcher": [
                "$gcc"
            ]
        },
        {
            "label": "clean",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceRoot}/demo"
            },
            "args": ["clean"]
        }
    ]
}